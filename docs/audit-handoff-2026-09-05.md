# Audit/design handoff — 5 September 2026

Starting revision: `1b407c06665e63e3b989bb83baaabe6e01793a75` on main. Candidate branch: `audit/design-hardening-2026-09-05`. No production deployment, secret changes or remote migration. The user requested a rundown before deploying; hold production for that review.

Implemented: event-led public design in `app/discovery.css`; shorter homepage; compact responsive event grids and search; clearer preview/cost labels; visible slideshow pause; tighter event/organiser copy; calendar-bound discovery filters; payment idempotency and uncertainty handling; event-correct fee quotes/total confirmation; Room notification block/suspension checks; owner/expiry-aware offline passes; temporary-password API permission enforcement; preference failure handling; targeted tooling dependency overrides and a complete-tree security gate.

Migration: apply `drizzle/0024_payment_attempt_idempotency.sql` before the updated payment endpoint. It adds a table; it does not delete existing data. The new checkout sends an idempotency key; older clients remain compatible without the new guarantee.

Final completion pass: Vinext upgraded from 0.0.50 to 1.0.0-beta.9 with @vitejs/plugin-rsc 0.5.34, removing image-size. Full npm audit now reports zero advisories. Privacy/support/transfer/return/recovery forms have bounded requests, error recovery, preserved unsent text and duplicate-submit guards for mutations. Transfers and returns use native modal dialogs. Added branded not-found/page-error recovery. Staff preparation uses a private HMAC decoy with a fail-closed missing-key response. Deployment provisions STAFF_LOGIN_DECOY_SECRET once via scripts/ensure-staff-login-secret.mjs; do not rotate existing account passwords. Added uncached /api/version with CI-verified RELEASE_SHA and Cloudflare version metadata.

Verification: lint, TypeScript, production build, 45 source/repository/Web Crypto checks, four generated-artifact checks, 85 Worker/D1 tests across 21 files, Drizzle check and git diff --check pass. Total automated checks: 134, excluding lint/types/migration/dry-run. Full dependency audit zero. Worker dry run passes at 594.25 KiB gzip / 152 client assets. Synthetic SQLite round-trip applies 25 migrations, restores 74 tables, preserves row counts and passes integrity/foreign-key checks. This is not a production backup rehearsal.

Release blockers: updated browser/mobile UI was not verified because preview access was blocked; the browser skill prohibits substituting unsupported browser automation, and the existing block must not be bypassed. Live provider and authenticated device acceptance remain unperformed. Runtime SHA instrumentation is implemented but no new revision has been deployed. No fresh production restore/load test or production secret verification. Do not treat mocked provider tests as live-provider readiness.

The previous open source findings (dependency path, login decoy, failed-request controls and not-found recovery) are corrected. Candidate verification workflow checks the exact PR head without production credentials or deployment. Browser suite remains authored but unexecuted for this revision. Existing untracked test-results/ was preserved and excluded from candidate source.

Release order after review and outstanding acceptance: capture production recovery bookmark, provision the new staff login key, apply additive migration 0024 before the new payment endpoint, deploy the exact checked commit, verify /api/version and critical flows. Existing tables are preserved. The old Worker can run with the additive table and new secret present. Keep the earlier known-good release available for rollback.

Party visual follow-up: replaced the cream discovery surfaces with a dark poster wall, brighter theme-aware dates/filters, larger uppercase hero titles and square desktop artwork. Increased hero photography coverage and used the featured event image behind the Room stage. Empty states, search, pagination and focus colours follow the dark palette. Homepage filters now receive the entire event collection before the six-card display limit, so a matching event cannot disappear just because it occurred after the first six records.

The Room now uses a generated photorealistic black-titanium phone raster around live HTML conversations, with a continuous rounded display, Dynamic Island/camera detail and an inset home indicator. Physical aspect ratio follows Apple's iPhone 16 Pro specification (71.5 / 149.6); this is a generated representation, not an Apple product photograph or a certified exact device model. Asset: public/devices/iphone-black-titanium.png. Built-in image generation prompt: straight-on black titanium iPhone 16 Pro hardware, thin rails, blank screen for live HTML, no text or UI. A background-removal edit retained a studio backdrop; CSS clips it at the phone rail instead of claiming the asset has transparency. Screen content and the existing 4.5-second scene timing remain functional HTML. The image is lazy-loaded and shared by both phone instances. Source render was visually inspected; the composed app remains unverified because the preview-access block is still in effect. Nothing has been deployed.

Visual follow-up validation: TypeScript, lint, all 43 existing UI source contracts, production build and all four generated-artifact checks passed. Updated the existing source contracts for the intended title scale, complete collection filtering and a decorative asset path; the restriction on visible device/mockup terminology remains. These checks do not certify pixel layout or replace browser acceptance.

Approved production release: PR #77 merged to 78cb5effaead6a2336072c39d2a0fa3e4528b9e1; workflow 33958592612 passed and Worker version 68d7da37-bbb3-4170-a7d8-4916615051fd went live. Runtime /api/version matched. Migration 0024 and staff privacy key provisioning succeeded. D1 recovery bookmark preserved; production export/isolated restore workflow 33958592566 passed. The live desktop hero, gallery, iPhone previews, discovery-to-checkout journey and payment method controls were inspected successfully. No payment or customer notification was submitted.

Post-release browser workflow 33958673442 found two causes across nine failing cases: a CSS-uppercase-sensitive/racing slideshow assertion, and low contrast on the static closed-sales message. The follow-up freezes the scene before comparing raw event text and presents closed sales as readable text without disabled-control opacity. Keep all browser accessibility gates enabled. Previous rollback Worker: e4d6b3b5-8f48-4b69-a5d9-295d98e931fc. Original untracked test-results/ remains excluded.
