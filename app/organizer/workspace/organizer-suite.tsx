'use client';
import Link from 'next/link';
import {useCallback,useState} from 'react';
import {useRouter,useSearchParams} from 'next/navigation';
import {LayoutDashboard,CalendarDays,Users,Send,Wallet,UserRoundCog,LogOut,ArrowUpRight} from 'lucide-react';
import type {StaffRole} from '../../../lib/admin-session';
import {preferredHostEvent} from '../../../lib/host-guide';
import BrandLogo from '../../brand-logo';
import WorkspaceJump from '../../admin/workspace-jump';
import LegacyWorkspace from './organizer-workspace';
import HostStart from './host-start';
import OrganizerAnalytics from '../analytics/organizer-analytics';
import {Audience,Guests,Money} from './suite-records';
import {Promote,Team} from './suite-promote';
import {Tickets,Questions,Duplicate,Draft} from './suite-events';
import {date,readable,useRemote,RemoteState,type EventItem} from './suite-shared';
import './suite.css';
const areas=[{id:'overview',label:'Overview',Icon:LayoutDashboard},{id:'events',label:'Events',Icon:CalendarDays},{id:'audience',label:'Audience',Icon:Users},{id:'promote',label:'Promote',Icon:Send},{id:'money',label:'Money',Icon:Wallet},{id:'team',label:'Team',Icon:UserRoundCog}];
const eventViews=[['overview','Overview'],['tickets','Tickets'],['guests','Guests'],['room','Room & VIP'],['door','Door'],['insights','Insights']];
type Navigate=(area:string,view?:string,event?:string)=>void;
function Submissions({navigate}:{navigate:Navigate}){const [open,setOpen]=useState(false),r=useRemote<{submissions:{id:string;title:string;status:string;reviewNote:string|null;createdAt:string;eventSlug:string|null}[]}>('submissions','all','',open);return <details className="suite-disclosure" onToggle={e=>setOpen(e.currentTarget.open)}><summary>Submission history</summary><RemoteState error={r.error} loading={!r.data} refresh={r.refresh}/><div className="suite-rows">{r.data?.submissions.map(x=><article key={x.id}><div><b>{x.title}</b><p>{readable(x.status)} · {date(x.createdAt)}</p>{x.reviewNote?<p>{x.reviewNote}</p>:null}</div>{x.eventSlug?<button onClick={()=>navigate('events','overview',x.eventSlug!)}>Manage event</button>:null}</article>)}</div>{r.data&&!r.data.submissions.length?<p>Your submissions will appear here.</p>:null}</details>;}
function EventWorkspace({event,view,navigate,onRefresh,actor,role}:{event:EventItem;view:string;navigate:Navigate;onRefresh:()=>void;actor:string;role:StaffRole}){
 const [visited,setVisited]=useState<string[]>([view]);
 if(!visited.includes(view))setVisited([...visited,view]);
 const open=(v:string)=>navigate('events',({start:'overview',details:'tickets',entry:'door',requests:'help'} as Record<string,string>)[v]??v,event.slug);
 return <><header className="suite-event-header"><div><p>{event.venue} · {date(event.startsAt)}</p><h1>{event.title}</h1></div>{event.status==='published'?<Link className="suite-button" href={`/event/${event.slug}`}>View event <ArrowUpRight size={16}/></Link>:<span className="suite-badge">{readable(event.status)}</span>}</header><nav className="suite-event-nav" aria-label="Event tools">{eventViews.map(([id,label])=><button key={id} aria-current={view===id?'page':undefined} onClick={()=>navigate('events',id,event.slug)}>{label}</button>)}<button aria-current={view==='help'?'page':undefined} onClick={()=>navigate('events','help',event.slug)}>Help</button></nav><div hidden={view!=='overview'}>{event.status==='draft'?<Draft event={event.slug} onSaved={onRefresh}/>:<HostStart eventSlug={event.slug} onOpen={open} active={view==='overview'}/>}<Duplicate event={event} onCreated={slug=>{onRefresh();navigate('events','overview',slug);}}/><details className="suite-disclosure"><summary>Guest questions</summary>{visited.includes('overview')?<Questions event={event.slug}/>:null}</details></div><div hidden={view!=='tickets'}>{visited.includes('tickets')?<Tickets event={event.slug}/>:null}</div><div hidden={view!=='guests'}>{visited.includes('guests')?<Guests event={event.slug}/>:null}</div><div hidden={view!=='door'}>{visited.includes('door')?<Team event={event.slug} door/>:null}</div><div hidden={view!=='insights'}>{visited.includes('insights')?<OrganizerAnalytics actor={actor} role={role} initialEvent={event.slug} embedded active={view==='insights'}/>:null}</div><div hidden={!['room','help','tickets'].includes(view)}>{visited.some(v=>['room','help','tickets'].includes(v))?<LegacyWorkspace actor={actor} role={role} embeddedEventSlug={event.slug} embeddedView={view==='room'?'room':view==='tickets'?'details':'requests'}/>:null}</div></>;
}
export default function OrganizerSuite({actor,role}:{actor:string;role:StaffRole}){
 const router=useRouter(),params=useSearchParams(),r=useRemote<{events:EventItem[]}>('events','all'),[seen,setSeen]=useState<string[]>([]);
 const area=areas.some(a=>a.id===params.get('area'))?params.get('area')!:'overview',view=[...eventViews.map(x=>x[0]),'help'].includes(params.get('view')??'')?params.get('view')!:'overview';
 const events=r.data?.events??[],requested=params.get('event'),selectedSlug=preferredHostEvent(events,requested==='all'?null:requested),selected=events.find(x=>x.slug===selectedSlug);
 const navigate:Navigate=useCallback((nextArea,nextView='overview',slug)=>{const p=new URLSearchParams({area:nextArea});if(slug??selectedSlug)p.set('event',slug??selectedSlug);if(nextArea==='events')p.set('view',nextView);router.push(`/organizer/workspace?${p.toString()}`,{scroll:false});},[router,selectedSlug]);
 const key=`${area}:${selectedSlug}`;
 if(selectedSlug&&!seen.includes(key))setSeen([...seen,key]);
 const totals=events.reduce((n,x)=>({admissions:n.admissions+x.admissions,arrivals:n.arrivals+x.arrivals,pending:n.pending+x.pendingRequests}),{admissions:0,arrivals:0,pending:0});
 return <main className="organizer-suite"><header className="suite-topbar"><Link href="/" className="night-brand-link"><BrandLogo/></Link><WorkspaceJump active="/organizer/workspace" role={role} compact/><div><span>{actor}</span><button aria-label="Sign out" onClick={async()=>{await fetch('/api/admin/session',{method:'DELETE'});router.push('/');router.refresh();}}><LogOut size={16}/></button></div></header><div className="suite-layout"><aside className="suite-sidebar"><p>Host workspace</p><nav aria-label="Organiser workspace">{areas.map(({id,label,Icon})=><Link key={id} href={`/organizer/workspace?area=${id}${selectedSlug?`&event=${selectedSlug}`:''}`} aria-current={area===id?'page':undefined}><Icon size={18}/><span>{label}</span></Link>)}</nav><div className="suite-secondary"><Link href="/organizer/assistant">Event desk</Link><Link href="/admin/account">Account settings</Link><Link href="/help">Help centre</Link></div></aside><div className="suite-content"><RemoteState error={r.error} loading={!r.data} refresh={r.refresh}/>{r.data?<>{area!=='audience'&&area!=='overview'&&events.length?<div className="suite-event-picker"><label htmlFor="suite-event">{area==='money'?'Report scope':'Your event'}</label><select id="suite-event" value={area==='money'&&requested==='all'?'all':selectedSlug} onChange={e=>navigate(area,view,e.target.value)}>{area==='money'?<option value="all">All my events</option>:null}{events.map(x=><option key={x.slug} value={x.slug}>{x.title}</option>)}</select><span>{area==='money'&&requested==='all'?'Across your assigned events':readable(selected?.status??'')}</span></div>:null}<div hidden={area!=='overview'}><section className="suite-section"><header><div><p className="suite-eyebrow">Overview</p><h1>Your night, in hand.</h1><p>What needs attention, and what’s coming next.</p></div><Link className="suite-button suite-button--primary" href="/organizer/submit">Submit a Night</Link></header><div className="suite-stats"><article><span>Your events</span><b>{events.length}</b></article><article><span>Admissions issued</span><b>{totals.admissions}</b></article><article><span>Checked in</span><b>{totals.arrivals}</b></article><article><span>RSVPs to review</span><b>{totals.pending}</b></article></div><div className="suite-rows">{events.slice(0,12).map(x=><article key={x.slug}><div><b>{x.title}</b><p>{date(x.startsAt)} · {readable(x.status)}</p>{x.pendingRequests?<small>{x.pendingRequests} RSVPs waiting for review</small>:null}</div><button onClick={()=>navigate('events','overview',x.slug)}>Manage event</button></article>)}{!events.length?<p>Your approved Nights and drafts will appear here. Check submission history for updates.</p>:null}</div><Submissions navigate={navigate}/></section></div>{area==='audience'||seen.some(x=>x.startsWith('audience:'))?<div hidden={area!=='audience'}><Audience/></div>:null}{area==='money'||seen.some(x=>x.startsWith('money:'))?<div hidden={area!=='money'}><Money event={requested==='all'?'all':selectedSlug||'all'}/></div>:null}{events.filter(x=>seen.some(k=>k.endsWith(`:${x.slug}`))||x.slug===selectedSlug).map(event=><div key={event.slug} hidden={event.slug!==selectedSlug}>{(area==='events'&&event.slug===selectedSlug)||seen.includes(`events:${event.slug}`)?<div hidden={area!=='events'}><EventWorkspace event={event} view={view} navigate={navigate} onRefresh={r.refresh} actor={actor} role={role}/></div>:null}{(area==='promote'&&event.slug===selectedSlug)||seen.includes(`promote:${event.slug}`)?<div hidden={area!=='promote'}><Promote event={event.slug}/></div>:null}{(area==='team'&&event.slug===selectedSlug)||seen.includes(`team:${event.slug}`)?<div hidden={area!=='team'}><Team event={event.slug}/></div>:null}</div>)}{!events.length&&['events','promote','team'].includes(area)?<section className="suite-section"><h2>Your first Night starts here.</h2><p>Submit an event to open these tools after review.</p><Link href="/organizer/submit">Submit a Night</Link></section>:null}</>:null}</div></div></main>;
}
